{
  "name": "src",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "prisma migrate deploy && node index.js",
    "seed:user": "node scripts/create_user.js",
    "seed:categories": "node scripts/create_categorias.js",
    "seed:publicaciones": "node scripts/create_publicaciones.js",
    "seed:all": "npm run seed:user && npm run seed:categories && npm run seed:publicaciones",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.22.1",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^7.0.10",
    "prisma": "^6.17.1"
  }
}
